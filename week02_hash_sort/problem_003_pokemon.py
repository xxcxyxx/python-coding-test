```python
"""
문제명: 폰켓몬
출처: Programmers
유형: Hash / Set / Greedy
난이도: Level 1
상태: Solved
날짜: 2026-06-14

문제 요약:
- 전체 N마리의 폰켓몬 중 N/2마리를 선택할 수 있다.
- 최대한 다양한 종류의 폰켓몬을 선택했을 때,
  선택할 수 있는 종류 수의 최댓값을 반환한다.

풀이 접근:
1. len(nums) // 2를 사용해 선택할 수 있는 폰켓몬 수를 구한다.
2. set(nums)를 사용해 전체 폰켓몬 종류 수를 구한다.
3. 선택할 수 있는 마릿수와 전체 종류 수 중 작은 값을 반환한다.

시간복잡도:
- O(N)
- set을 생성하면서 nums를 한 번 순회한다.

공간복잡도:
- O(N)
- 서로 다른 폰켓몬 종류를 set에 저장한다.

배운 점:
- set을 사용하면 중복을 제거하고 서로 다른 종류의 개수를 구할 수 있다.
- 선택 가능한 개수보다 종류가 많아도 모두 선택할 수는 없다.
- 전체 종류가 선택 가능한 개수보다 적다면 존재하는 종류 수가 최댓값이 된다.
- 두 값 중 작은 값을 구할 때 min()을 사용할 수 있다.
"""

def solution(nums):
    select_count = len(nums) // 2
    type_count = len(set(nums))

    return min(select_count, type_count)
```
